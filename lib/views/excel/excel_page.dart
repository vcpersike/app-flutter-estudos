import 'package:flutter/material.dart';

class ExcelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Visualizador de Excel')),
      body: Center(
        child: Text('Tela de visualização Excel'),
      ),
    );
  }
}
